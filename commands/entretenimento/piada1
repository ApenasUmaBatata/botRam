const Discord = require("discord.js");
const db = require("quick.db")
module.exports = {
  config: {
    name: "piada",
    aliases: ["piadas", "fun"]
  },
  run: async (bot, message, args, member) => {
      let fra = db.get(`frase_${member.guild.id}`);


    message.channel.send(fra);
  }
};
