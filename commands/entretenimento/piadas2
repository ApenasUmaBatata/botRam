const Discord = require("discord.js");
onst db = require("quick.db");
module.exports = {
  config: {
    name: "addpiada",
    aliases: ["apiada"]
  },
  run: async (bot, message, args, member) => {

  }
}
